# Frontend Service

- `app/`: React-Quellcode (public/src/package)
- `Dockerfile`: Build + NGINX Runtime
- `nginx.conf`: SPA-Routing und statische Assets

Build-Kontext ist ausschließlich `AWS_DEPLOY/frontend`.
